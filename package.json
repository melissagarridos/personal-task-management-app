{
  "name": "estudio-dieguito",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "json-server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "@tailwindcss/vite": "^4.3.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
